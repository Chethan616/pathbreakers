import QtQuick

// Uppercase, tracked section banner - the reference design's panel heading
// style (there: "HEART RATE" / "BREATH RATE").
Text {
    color: Theme.ink
    font.family: Theme.fontSans
    font.pixelSize: Theme.headingSection
    font.weight: Font.DemiBold
    font.letterSpacing: Theme.headingSectionTracking
}
